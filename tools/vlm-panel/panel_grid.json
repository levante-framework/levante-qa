{
  "task": "trog",
  "spec": "cypress/e2e/trog/vlm_agent.cy.ts",
  "provider": "gemini",
  "language": "en",
  "temperature": 0.8,
  "personaAbility": "irt",
  "repeats": 4,
  "models": [
    "gemini-3.5-flash-lite",
    "gemini-3.6-flash"
  ],
  "ages": [6, 8, 10, 12],
  "_notes": "Each respondent = one full TROG run = one cell (model x age x repeat). Default 2 x 4 x 4 = 32 respondents (flash-lite + flash only). Ages end at 12 (was 13) to match denser ability n in age_task_ability.json. Pro off by default. Prefer: capture assets once (--capture-assets) then --replay (no Cypress UI). Stimulus never altered."
}
