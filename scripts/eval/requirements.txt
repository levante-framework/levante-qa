# SOTA translation-evaluation pipeline (levante-qa/scripts/eval)
#
# COMET-QE (neural quality estimation). Pulls torch + pytorch-lightning + HF hub.
unbabel-comet>=2.2.2

# Multilingual sentence embeddings (direct cross-lingual similarity + centroids).
sentence-transformers>=2.6.0

# Numerics for embeddings, centroids, and the validation statistics.
numpy>=1.24.0

# Logistic-regression blend in ensemble_eval.py (also pulled by sentence-transformers).
scikit-learn>=1.3.0

# Hugging Face hub client (login for the gated COMET-QE model; comet depends on
# it transitively, pinned here so `huggingface-cli login` is available).
huggingface_hub>=0.20.0

# Progress bars for the (sequential) LLM pass.
tqdm>=4.65.0

# NOTE: the Gemini LLM-as-a-judge calls the REST API directly via the stdlib
# (urllib), mirroring levante_translations/translation_grading. We deliberately
# do NOT depend on `google-generativeai` (end-of-life 2025-11-30) or any other
# Gemini SDK, so this pipeline cannot be broken by an SDK deprecation.
